# Wordle_Game
___
## Installation
```python
pip install sqlite3
pip install hashlib
pip install soupsieve
pip install apscheduler
pip install datetime
pip install logging
pip install asyncio
pip install aiogram
```
